stages:
  - build
  - test

install_dependencies:
  stage: build
  image: node:10
  script:
    - yarn install
  artifacts:
    paths:
      - node_modules/

jest:
  stage: test
  image: node:10
  dependencies:
    - install_dependencies
  script:
    - yarn test:unit

cypress:
  stage: test
  image: cypress/base:10
  dependencies:
    - install_dependencies
  script:
    - yarn test:e2e --headless
  artifacts:
    paths:
      - tests/e2e/screenshots
      - tests/e2e/videos